namespace Lombiq.Hosting.Azure.ApplicationInsights.Constants
{
    public static class ResourceNames
    {
        private const string Prefix = FeatureIds.Area + ".";

        public const string TrackingScript = Prefix + nameof(TrackingScript);
    }
}
